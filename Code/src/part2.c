#include "fat32.h"

/*
int mkdir(unsigned int current_cluster, char * dirname)
{
	int empty_dir_entry;
	struct FAT32DirBlock dblock;

	empty_dir_entry = findEmptyDirEntry(current_cluster);
	if(empty_dir_entry > 0)
	{

	}

}

*/